When the project is updated or new features are added:
- Rule 1: Always check if the `trickle/notes/README.md` file needs to be updated.
- Rule 2: Keep the README.md content synchronized with the actual application features and architecture.